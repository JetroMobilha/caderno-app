# Tarefas: Reforço da Sincronização Colaborativa Reativa

- [x] **Backend (Laravel)**
    - [x] Modificar `PageUpdated.php` para enviar apenas sinalização leve.
    - [x] Atualizar `SyncController.php` para disparar `PageUpdated` e validar roles no `pushNotebooks`.
- [x] **Frontend (Flutter)**
    - [x] Validar mapeamento de eventos em `realtime_service.dart`.
    - [x] Ajustar lógica de recepção de sinal em `canvas_controller.dart`.
- [x] **Verificação**
    - [x] Testar reatividade de sinalização.
    - [x] Validar integridade de dados após sincronização forçada por sinal.
    - [ ] Testar reatividade de sinalização.
    - [ ] Validar integridade de dados após sincronização forçada por sinal.
