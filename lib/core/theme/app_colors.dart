import 'package:flutter/material.dart';

class AppColors {
  // Cores Principais
  static const Color primary = Color(0xFF0F4C5C);
  static const Color accent = Color(0xFF27AE60); // Verde para sucessos/online
  static const Color error = Colors.redAccent;

  // Fundos e Papel
  static const Color background = Color(0xFFD6D6D6);
  static const Color paper = Color(0xFFFDFBF7);

  // Textos
  static const Color textDark = Color(0xFF1A1A24);
  static const Color textLight = Colors.white;
  static const Color textMuted = Colors.black45;
}