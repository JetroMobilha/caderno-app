name: caderno_digital_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter

  flutter_riverpod: ^2.6.1
  http: ^1.6.0
  shared_preferences: ^2.5.5
  connectivity_plus: ^7.1.1
  intl: ^0.19.0
  google_fonts: ^8.1.0
  uuid: ^4.5.3
  vector_math: ^2.1.4
  image_picker: ^1.2.3
  flutter_secure_storage: ^10.3.1
  cached_network_image: ^3.4.1
  dart_pusher_channels: ^1.3.1
  permission_handler: ^11.3.0
  record: ^5.0.0 
  audioplayers: ^6.1.0
  reorderable_grid_view: ^2.2.8

  drift: ^2.34.2
  drift_flutter: ^0.3.1
  sqlite3: ^3.5.0
  sqlite3_flutter_libs: ^0.6.0+eol
  path_provider: ^2.1.6
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter

  flutter_lints: ^6.0.0
  mockito: ^5.4.4
  drift_dev: ^2.34.4
  build_runner: ^2.4.8

dependency_overrides:
  record_linux: 0.7.1 # 🚀 Força compatibilidade

flutter:
  uses-material-design: true
